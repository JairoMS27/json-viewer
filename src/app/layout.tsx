import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "JSON Viewer - Simple and Beautiful",
  description: "A modern JSON viewer and formatter with syntax highlighting and a simplified view",
  authors: [{ name: "JairoMS27", url: "https://github.com/JairoMS27" }],
  keywords: ["JSON", "viewer", "formatter", "parser", "editor", "syntax highlighting"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
